# 주간 ECC 감사 보고서 — 2026-03-29

## 1. 보안 스캔

### 발견사항

#### 🔴 CRITICAL — .mcp.json에 Google Stitch API 키 하드코딩 + Git 추적됨
- **파일**: `.mcp.json`
- **내용**: `"X-Goog-Api-Key": "AQ.Ab8RN6Kk6kNCuzh_M0uYfw42ZTQzGLOPTY1w5SiC3AuQ9MMELA"`
- **문제**: 키가 Git에 커밋되어 있음 (`git ls-files`로 추적 확인). `.gitignore`에 미등록.
- **히스토리**: 커밋 `efac2e3`, `7b7e283`에 노출. GitHub 공개 저장소라면 키 즉시 폐기 필요.

#### 🟡 MEDIUM — smoke-test.sh에 기본 자격증명 하드코딩
- **파일**: `.claude/hooks/smoke-test.sh` (line 9)
- **내용**: `PASSWORD="${3:-admin1234}"`, `"password":"admin1234"`
- **평가**: 기본값(fallback)으로 사용되며 실제 운영 비밀번호와 일치할 경우 위험. 현재 working-state.md에서 superadmin 비밀번호가 `admin1234`임을 확인 → 일치함.

#### 🟡 MEDIUM — E2E 테스트 프롬프트에 OAuth CLI 토큰 참조
- **커밋**: `8b83467` ("fix: add real OAuth CLI token to E2E prompts"), `ff4b455`
- **위치**: `desktop-test-prompts/chrome-e2e/` 디렉토리
- **평가**: 실제 `sk-ant-cli-*` 토큰이 마크다운 프롬프트 파일에 포함된 것으로 추정. 현재 파일 내용은 평문 토큰 없음(검색 결과 0건)이지만 과거 커밋에 노출 가능성 있음.

#### 🟢 양호 — governance-capture.js 시크릿 탐지
- AWS 키, GitHub 토큰, Anthropic API/CLI 키, Neon DSN, JWT 패턴 탐지 활성화
- `instincts/observe.sh`에 시크릿 스크러빙 로직 포함 (REDACTED 처리)

#### 🟢 양호 — .env 파일 GitIgnore 처리
- `.gitignore`에 `.env`, `.env.*` 패턴 등록 완료

### 위험도 요약
| 항목 | 위험도 | 즉시 조치 필요 |
|------|--------|--------------|
| .mcp.json API 키 Git 노출 | 🔴 CRITICAL | 예 |
| smoke-test 기본 비밀번호 | 🟡 MEDIUM | 권장 |
| E2E OAuth 토큰 이력 | 🟡 MEDIUM | 이력 확인 필요 |

---

## 2. 하니스 감사

### hooks.json 상태

**등록된 이벤트**: SessionStart, PreToolUse, PreCompact, PostCompact, PostToolUse, Stop

| 이벤트 | 훅 수 | 상태 |
|--------|------|------|
| SessionStart | 3 | 정상 (cleanup + submodule update + ECC session-start) |
| PreToolUse | 5 | 정상 (tsc, party-log-verify, party-mode-compliance, toast-check, ECC governance) |
| PreCompact | 1 | 정상 (pre-compact.js) |
| PostCompact | 1 | 정상 (post-compact-save.sh) |
| PostToolUse | 3 | 정상 (post-edit-console-warn, governance, continuous-learning) |
| Stop | 5 | 정상 (update-log, session-reminder, session-end, cost-tracker, dream-pending) |

#### ⚠️ 발견된 이슈

1. **경로 불일치 (하드코딩 `/home/ubuntu/`)**: hooks.json 전체에 `/home/ubuntu/corthex-v2/` 경로 19개 사용. 현재 실행 환경은 `/home/user/corthex-v2/`. 심볼릭 링크나 환경 동기화로 작동 중이나 이식성 위험.

2. **cross-check.sh 실행 권한 없음**: `-rw-r--r--` (실행 권한 없음). BMAD 워크플로우에서 `bash .claude/hooks/cross-check.sh`로 직접 실행하므로 현재는 동작하나, hook 직접 호출 시 실패 가능.

3. **smoke-test.sh 실행 권한 없음**: `-rw-r--r--`. 마찬가지로 `bash` 명시 호출 시만 동작.

4. **continuous-learning-v2 훅**: `bash ~/.claude/skills/continuous-learning-v2/hooks/observe.sh` — 해당 경로(`~/.claude/skills/continuous-learning-v2/`) 미존재. PreToolUse/PostToolUse마다 silently fail 중. 에러 미노출이나 학습 데이터 누락 발생.

### settings.json 상태
- 모든 주요 도구 허용: `Bash(*)`, `Read(*)`, `Write(*)`, `Edit(*)`, `Agent(*)`, `Skill(*)`, `TeamCreate(*)` 등
- `deny` 항목 없음 → 최대 권한 모드 (개발 환경으로는 적합, 보안 관점에서는 감사 추적 의존)

### MCP 설정 상태 (.mcp.json)
| 서버 | 타입 | 상태 |
|------|------|------|
| stitch (Google) | HTTP | ⚠️ API 키 하드코딩 (CRITICAL) |
| playwright | stdio (npx) | 정상 |
| code-review-graph | stdio (uvx) | 정상 |

---

## 3. 스킬 건강

### 총 스킬 수
- **프로젝트 로컬** (`.claude/skills/`): 2개
  - `korean-cover-letter-writer`
  - `ui-ux-pro-max`
- **글로벌** (`~/.claude/skills/`): 1개 확인됨
  - `session-start-hook`
- **ECC skills-health.js 결과**: `"skills": []` — 추적 데이터 없음

### 미사용 스킬
- `skills-health.js` 실행 결과 스킬 런 데이터 0건. 스킬 실행 이력이 추적되지 않고 있음.
- 원인: `continuous-learning-v2` 훅 경로 누락으로 관측 데이터 수집 불가.

### 하락 추세
- 데이터 수집 자체가 안 되고 있어 추세 분석 불가.
- `skills-health.js --dashboard`도 빈 데이터 반환.

### 스킬 시스템 건강도 요약
| 지표 | 상태 |
|------|------|
| 스킬 실행 추적 | ❌ 비작동 (경로 누락) |
| 스킬 건강 대시보드 | ❌ 데이터 없음 |
| 로컬 스킬 파일 | ✅ 2개 정상 존재 |
| ECC hooks/lib/skill-evolution | ✅ 코드 존재 |

---

## 4. 규칙 추출

### 최근 세션 패턴 분석 (2026-03-21~29 커밋/로그 기반)

#### 새 규칙 후보

**R-01: E2E 테스트 프롬프트에 실제 토큰 절대 포함 금지**
- 근거: `8b83467` 커밋 ("add real OAuth CLI token to E2E prompts") 패턴. 토큰은 환경변수나 `.credentials.json`(gitignored)에서 런타임 로드.
- 규칙: `desktop-test-prompts/` 파일에 `sk-ant-` 패턴 감지 시 커밋 차단.

**R-02: .mcp.json을 .gitignore에 추가 필수**
- 근거: 현재 API 키 노출 사고. MCP 설정에는 API 키가 포함될 수 있음.
- 규칙: `.mcp.json`을 `.gitignore`에 등록, 예시 파일 `.mcp.json.example` 사용.

**R-03: 운영 기본 비밀번호와 스크립트 기본값 분리**
- 근거: smoke-test의 `admin1234` 기본값이 실제 운영 superadmin과 동일.
- 규칙: smoke-test 기본 비밀번호는 `test-default-only`처럼 실제 비밀번호와 구분 가능한 값 사용.

**R-04: hooks.json 경로를 절대경로 대신 상대경로/환경변수로**
- 근거: `/home/ubuntu/` 하드코딩 19건 — 환경 이동 시 전체 수정 필요.
- 규칙: `$PROJECT_ROOT` 또는 `$HOME` 환경변수 기반 경로 사용.

**R-05: 스킬 관측 훅 경로 유효성 검증 게이트**
- 근거: `continuous-learning-v2/hooks/observe.sh` 누락으로 학습 데이터 수집 중단.
- 규칙: SessionStart에서 필수 훅 경로 존재 여부 체크 + 경고 출력.

**R-06: 3h maintenance 커밋 = ECC 정상 작동 증거**
- 근거: `2026-03-29`에 `chore(ecc): 3h maintenance` 커밋 3건 존재. dream 스킬 자동 정리 작동 확인.
- 메모: 이 패턴은 정상이며 억제 불필요.

---

## 5. 추천 액션

### 즉시 처리 (이번 주)

1. **🔴 [보안] .mcp.json을 .gitignore에 추가**
   ```bash
   echo ".mcp.json" >> .gitignore
   git rm --cached .mcp.json
   cp .mcp.json .mcp.json.example
   # .mcp.json.example에서 실제 키 제거
   git add .gitignore .mcp.json.example
   git commit -m "security: remove .mcp.json from tracking, add to .gitignore"
   ```

2. **🔴 [보안] Google Stitch API 키 재발급**
   - 현재 키 `AQ.Ab8RN6Kk6kNCuzh_M0uYfw42ZTQzGLOPTY1w5SiC3AuQ9MMELA` 폐기
   - Google Cloud Console에서 새 키 발급 → 로컬 `.mcp.json`에만 저장

3. **🟡 [보안] E2E 프롬프트 토큰 히스토리 확인**
   ```bash
   git log --all -S "sk-ant-cli" -- "desktop-test-prompts/"
   ```
   토큰 발견 시 해당 Claude CLI 토큰 재발급.

### 이번 달 처리

4. **🟡 [안정성] continuous-learning-v2 훅 경로 수정**
   - `hooks.json`에서 `bash ~/.claude/skills/continuous-learning-v2/hooks/observe.sh` 경로 확인
   - 실제 경로로 수정 또는 해당 훅 설치

5. **🟡 [이식성] hooks.json 경로 환경변수화**
   - `/home/ubuntu/corthex-v2/` → `$HOME/corthex-v2/` 또는 실행 시 동적 해석

6. **🟢 [품질] cross-check.sh, smoke-test.sh 실행 권한 추가**
   ```bash
   chmod +x .claude/hooks/cross-check.sh .claude/hooks/smoke-test.sh
   ```

7. **🟢 [거버넌스] R-01~R-05 규칙을 governance-capture.js에 추가**
   - E2E 토큰 패턴 탐지 규칙 추가
   - `.mcp.json` 커밋 시도 차단 규칙 추가

---

## 6. 감사 메타데이터

| 항목 | 값 |
|------|---|
| 감사 일시 | 2026-03-29 |
| 감사 범위 | `.claude/` 전체, `.mcp.json`, `hooks.json`, `settings.json`, git 이력 20건 |
| 총 발견사항 | CRITICAL 1건, MEDIUM 2건, INFO 4건 |
| 스킬 추적 상태 | ❌ 비활성 (데이터 수집 중단) |
| 다음 감사 예정 | 2026-04-05 |
