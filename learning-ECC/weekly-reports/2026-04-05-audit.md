# 주간 ECC 감사 보고서 — 2026-04-05

> Auditor: Claude Sonnet 4.6 (automated)
> Scope: `.claude/` 전체 — hooks, skills, settings, memory, logs

---

## 1. 보안 스캔

### 발견사항

| # | 파일 | 내용 | 판단 |
|---|------|------|------|
| S-1 | `.claude/hooks/smoke-test.sh:9` | `PASSWORD="${3:-admin1234}"` 하드코딩 | 저위험 — 테스트 기본값, 파라미터로 오버라이드 가능 |
| S-2 | `.claude/memory/claude-agent-sdk-api-reference.md:167` | `"sk-ant-..."` 문자열 | 무해 — placeholder 예시 (`...` 패턴) |
| S-3 | `settings.json` | `Bash(*) / Read(*) / Write(*) / Edit(*)` 전부 allow, deny 목록 없음 | 중위험 — 완전 개방 권한, 의도적 설계인지 확인 필요 |

### 긍정 확인
- `governance-capture.js`: `AKIA*/sk-ant-*/ghp_*/-----BEGIN PRIVATE KEY-----` 등 실제 시크릿 패턴 실시간 차단 ✓
- `instincts/observe.sh`: 도구 I/O에서 시크릿 자동 redact (`[REDACTED]`) ✓
- `detect-project.sh`: git remote URL에서 embedded credentials 자동 strip ✓
- `.env` / `.pem` / `.key` 파일 접근 시 governance-capture가 경고 발생 ✓

### 위험도
**전체: 저위험.** 실제 유출된 시크릿 없음. 보안 탐지 레이어가 정상 작동 중.

---

## 2. 하니스 감사

### hooks 상태

#### 🔴 CRITICAL: 경로 불일치 — hooks.json 전 훅 비작동

`hooks.json`의 모든 절대경로가 `/home/ubuntu/corthex-v2/...` 로 하드코딩되어 있으나, 현재 실행 환경은 `/home/user/corthex-v2/` 임. **hooks.json에 등록된 22개 명령어 중 18개가 현재 환경에서 실행 실패.**

영향받는 훅:
```
SessionStart: cleanup.sh, submodule update, session-start.js
PreToolUse:   pre-commit-tsc.sh, party-log-verify.sh, party-mode-compliance.js,
              toast-without-api-check.sh, doc-file-warning.js, suggest-compact.js,
              governance-capture.js
PreCompact:   pre-compact.js
PostCompact:  post-compact-save.sh
PostToolUse:  post-edit-console-warn.js, governance-capture.js
Stop:         auto-update-log.sh, session-end-reminder.sh, session-end.js, cost-tracker.js
```

`$HOME` 참조인 `bash ~/.claude/skills/continuous-learning-v2/hooks/observe.sh` 와 `bash $HOME/.claude/skills/dream/should-dream.sh`는 경로 방식은 올바르나, **해당 스킬 디렉토리 자체가 존재하지 않음** → 이 두 훅도 매 실행마다 실패.

#### 훅별 상세

| 훅 | 등록 여부 | 경로 작동 | 의존 스킬 존재 |
|----|-----------|-----------|----------------|
| cleanup.sh | ✓ SessionStart | ✗ ubuntu 경로 | — |
| session-start.js (ECC) | ✓ SessionStart | ✗ ubuntu 경로 | — |
| pre-commit-tsc.sh | ✓ PreToolUse(Bash) | ✗ ubuntu 경로 | — |
| party-log-verify.sh | ✓ PreToolUse(Bash) | ✗ ubuntu 경로 | — |
| party-mode-compliance.js | ✓ PreToolUse(Bash) | ✗ ubuntu 경로 | — |
| governance-capture.js | ✓ Pre+PostToolUse | ✗ ubuntu 경로 | — |
| observe.sh (instincts) | ✓ Pre+PostToolUse | ~ $HOME 참조 | ✗ continuous-learning-v2 없음 |
| pre-compact.js | ✓ PreCompact | ✗ ubuntu 경로 | — |
| post-compact-save.sh | ✓ PostCompact | ✗ ubuntu 경로 | — |
| session-end.js | ✓ Stop | ✗ ubuntu 경로 | — |
| cost-tracker.js | ✓ Stop | ✗ ubuntu 경로 | — |
| dream/should-dream.sh | ✓ Stop | ~ $HOME 참조 | ✗ dream 없음 |

### 설정 이슈

| # | 파일 | 이슈 | 권고 |
|---|------|------|------|
| H-1 | `hooks.json` | `/home/ubuntu` 하드코딩 전체 | 환경변수 `$HOME` 또는 `$CLAUDE_PROJECT_DIR` 로 교체 |
| H-2 | `hooks.json` | `continuous-learning-v2` 스킬 없음 | 글로벌 스킬 설치 또는 훅에서 제거 |
| H-3 | `hooks.json` | `dream` 스킬 없음 | 글로벌 스킬 설치 또는 훅에서 제거 |
| H-4 | `settings.json` | deny 목록 없음, Bash(*) 전부 허용 | 의도적이면 CLAUDE.md에 명시; 아니면 위험 Bash 커맨드 deny 추가 |
| H-5 | `settings.json` | MCP 서버 설정 없음 (`{}`) | Stitch MCP, GitHub MCP 등 재등록 필요한지 검토 |

---

## 3. 스킬 건강

### 총 스킬 수: 2개 (프로젝트 로컬)

| 스킬 | 버전 | 7일 성공률 | 30일 성공률 | 트렌드 | 마지막 실행 |
|------|------|-----------|------------|--------|------------|
| `korean-cover-letter-writer` | v1 | n/a | n/a | stable | 기록 없음 |
| `ui-ux-pro-max` | v1 | n/a | n/a | stable | 기록 없음 |

### 글로벌 스킬 (`~/.claude/skills/`)
- `session-start-hook` — 1개만 확인됨
- `continuous-learning-v2` — **없음** (hooks.json에서 참조 중)
- `dream` — **없음** (hooks.json Stop 훅에서 참조 중)

### 미사용 스킬
- `korean-cover-letter-writer`: 실행 이력 없음. 한국어 자소서 작성 기능 — 이 프로젝트(CORTHEX)에서는 사용 빈도 낮을 것으로 예상
- `ui-ux-pro-max`: 실행 이력 없음. UXUI 리디자인 Phase 7에서 활발히 사용할 예정이므로 유지

### 하락 추세
- 없음 (실행 데이터 자체가 없어 추세 판단 불가)
- **근본 원인**: hooks.json 경로 오류로 cost-tracker.js, session-end.js 등이 실행 안 되어 `.claude/metrics/` 디렉토리 자체가 생성되지 않음

---

## 4. 규칙 추출

최근 세션 패턴(`.claude/logs/`, `.claude/updates/`, `.claude/memory/`, CLAUDE.md) 분석 결과:

### 신규 규칙 후보

| ID | 패턴 근거 | 제안 규칙 | 우선순위 |
|----|-----------|-----------|---------|
| R-1 | hooks.json 경로 오류로 전 훅 비작동 | "hooks.json 수정 시 절대경로 `/home/*/` 금지. 반드시 `$HOME` 환경변수 사용" | 🔴 즉시 |
| R-2 | 2026-03-25 working-state "없다/있다" 오답 사건 | "패키지/스킬 존재 여부는 `ls` 직접 확인 후 보고. 메모리 추측 금지" (이미 CLAUDE.md에 있으나 반복 위반됨 → 강화 필요) | 🟠 높음 |
| R-3 | cost-tracker.js 비작동으로 비용 추적 불가 | "세션 시작 시 hooks 동작 여부 spot-check: `node .claude/hooks/ecc/hooks/cost-tracker.js < /dev/null`" | 🟡 중간 |
| R-4 | MCP 서버 설정이 settings.json에 없음 | "MCP 서버 목록을 CLAUDE.md 또는 별도 `mcp-registry.md`에 관리 → 환경 재설정 시 체크리스트로 활용" | 🟡 중간 |
| R-5 | quality-gate.js가 hooks.json에 등록되지 않음 (파일은 존재) | "ecc/hooks/에 파일이 있으나 hooks.json에 미등록된 훅 목록 주기적 확인 (quality-gate.js, evaluate-session.js, mcp-health-check.js, run-with-flags.js)" | 🟡 중간 |

### 패턴 관찰
- **"완료" 거짓 보고 패턴** — 이미 CLAUDE.md에 규칙화됨. 반복 위반은 에이전트 교육 문제로 규칙 수 늘리는 것보다 검증 게이트 자동화가 더 효과적.
- **party-mode-compliance.js**: 훅 자체는 잘 설계되어 있으나 hooks.json 경로 오류로 작동 안 함 → 파이프라인 품질 보호 기능 사실상 없는 상태.

---

## 5. 추천 액션

### 🔴 즉시 (이번 세션)

1. **hooks.json 경로 수정** — `/home/ubuntu/corthex-v2/` → `/home/user/corthex-v2/` 전체 교체
   - 또는 더 나은 방법: `$HOME/corthex-v2/` 로 환경 독립적으로 변경
   - 영향: 22개 명령어 중 18개 복원 (tsc 검증, party mode, governance, 비용추적 등)

2. **누락 스킬 처리** — hooks.json에서 `continuous-learning-v2`와 `dream` 참조 제거 또는 스킬 설치
   - 현재 Stop 훅 2개가 매 세션 종료마다 오류 발생 중

### 🟠 이번 주

3. **미등록 ECC 훅 검토** — 아래 파일들이 존재하나 hooks.json에 없음:
   - `ecc/hooks/quality-gate.js` — PostToolUse(Edit) 에 추가 검토
   - `ecc/hooks/evaluate-session.js` — Stop 훅으로 추가 검토
   - `ecc/hooks/mcp-health-check.js` — SessionStart 에 추가 검토

4. **MCP 서버 현황 파악** — `~/.claude/settings.json`에 mcpServers가 비어 있음. Stitch MCP, GitHub MCP 등 재등록 필요 여부 확인

### 🟡 다음 주

5. **hooks.json 경로 자동 검증 스크립트** — 신규 훅 추가 시 경로 존재 여부를 자동 체크하는 `validate-hooks.sh` 작성 (cross-check.sh에 통합 가능)

6. **스킬 실행 데이터 축적** — metrics/ 디렉토리가 생성되어 있지 않아 스킬 건강 지표가 전부 n/a. 훅 경로 수정 후 자연스럽게 축적됨

7. **`korean-cover-letter-writer` 필요성 재검토** — CORTHEX 프로젝트 성격상 사용 빈도 낮을 것으로 예상. 프로젝트 로컬 스킬에서 글로벌로 이동 검토

---

## 요약

| 카테고리 | 상태 | 주요 이슈 |
|----------|------|-----------|
| 보안 | 🟢 양호 | 실제 시크릿 유출 없음, 탐지 레이어 정상 |
| 훅 인프라 | 🔴 중단 | hooks.json 경로 오류로 대부분 비작동 |
| 스킬 | 🟡 보통 | 2개 정상이나 실행 이력 없음 |
| 규칙/거버넌스 | 🟡 보통 | CLAUDE.md 규칙 충분하나 훅 비작동으로 자동 강제 불가 |

**최우선 조치**: hooks.json 경로 수정 1개로 18개 보호 기능이 한 번에 복원됩니다.
