# 주간 ECC 감사 보고서 — 2026-04-12

> Auditor: Claude Sonnet 4.6 (automated)
> Scope: `.claude/` 전체 — hooks, skills, settings, memory, logs, commit history
> 이전 보고서 대비 변경사항 추적: `2026-04-05-audit.md` 참조

---

## 1. 보안 스캔

### 발견사항

| # | 파일 | 내용 | 변화 | 위험도 |
|---|------|------|------|--------|
| S-1 | `hooks/smoke-test.sh:9` | `PASSWORD="${3:-admin1234}"` 하드코딩 | ⚠️ 지난주 대비 미수정 | 저위험 — 테스트 기본값, 파라미터 오버라이드 가능 |
| S-2 | `hooks/ecc/hooks/governance-capture.js` | `ECC_GOVERNANCE_CAPTURE=1` 플래그 미설정 — 보안 캡처 비활성 | 🆕 신규 발견 | 중위험 — 시크릿 탐지 레이어가 코드 내 존재하나 실제 실행 안 됨 |
| S-3 | `settings.json` | `Bash(*) / Read(*) / Write(*) / Edit(*)` 전부 allow, deny 목록 없음 | ⚠️ 지난주 대비 미수정 | 중위험 — 의도적 설계면 CLAUDE.md에 명시 필요 |

### 긍정 확인
- `governance-capture.js`: AWS 키, JWT, GitHub 토큰, Neon DSN, Anthropic 키 등 8개 패턴 등록 ✓
- `instincts/observe.sh`: 도구 I/O에서 시크릿 자동 redact (`[REDACTED]`) ✓
- `.claude/` 전체 스캔 결과 실제 유출된 시크릿 없음 ✓
- `governance-capture.js` APPROVAL_COMMANDS: `git push --force`, `rm -rf`, `DROP TABLE` 등 위험 명령어 패턴 커버 ✓

### 위험도
**전체: 저위험.** 실제 유출 없음. 단, governance-capture 기능 자체가 비활성 상태 → 보안 이벤트 로그 전무.

---

## 2. 하니스 감사

### hooks 상태

#### 🔴 CRITICAL (2주 연속): hooks.json 경로 불일치 — 미수정

`hooks.json` 전체 명령어 중 19개가 `/home/ubuntu/corthex-v2/...` 하드코딩. 현재 환경은 `/home/user/corthex-v2/`.
**지난주(2026-04-05) 동일 이슈 보고 후 7일째 미조치.**

영향받는 훅 (변화 없음):
```
SessionStart: cleanup.sh, submodule update, session-start.js
PreToolUse:   pre-commit-tsc.sh, party-log-verify.sh, party-mode-compliance.js,
              toast-without-api-check.sh, doc-file-warning.js, suggest-compact.js,
              governance-capture.js
PreCompact:   pre-compact.js
PostCompact:  post-compact-save.sh
PostToolUse:  post-edit-console-warn.js, governance-capture.js
Stop:         auto-update-log.sh, session-end-reminder.sh, session-end.js, cost-tracker.js
```

`$HOME` 참조 훅 2개: `continuous-learning-v2/hooks/observe.sh`, `dream/should-dream.sh` — 스킬 디렉토리 미존재로 매 세션 종료마다 오류.

#### 훅별 상태표 (업데이트)

| 훅 | 등록 | 경로 | 스킬 존재 | 지난주 대비 |
|----|------|------|----------|-----------|
| cleanup.sh | ✓ SessionStart | ✗ ubuntu 경로 | — | 미수정 |
| session-start.js | ✓ SessionStart | ✗ ubuntu 경로 | — | 미수정 |
| pre-commit-tsc.sh | ✓ PreToolUse(Bash) | ✗ ubuntu 경로 | — | 미수정 |
| party-log-verify.sh | ✓ PreToolUse(Bash) | ✗ ubuntu 경로 | — | 미수정 |
| party-mode-compliance.js | ✓ PreToolUse(Bash) | ✗ ubuntu 경로 | — | 미수정 |
| governance-capture.js | ✓ Pre+PostToolUse | ✗ ubuntu 경로 | — | 미수정 |
| observe.sh (instincts) | ✓ Pre+PostToolUse | ~ $HOME 참조 | ✗ cl-v2 없음 | 미수정 |
| pre-compact.js | ✓ PreCompact | ✗ ubuntu 경로 | — | 미수정 |
| post-compact-save.sh | ✓ PostCompact | ✗ ubuntu 경로 | — | 미수정 |
| session-end.js | ✓ Stop | ✗ ubuntu 경로 | — | 미수정 |
| cost-tracker.js | ✓ Stop | ✗ ubuntu 경로 | — | 미수정 |
| dream/should-dream.sh | ✓ Stop | ~ $HOME 참조 | ✗ dream 없음 | 미수정 |
| quality-gate.js | ✗ 미등록 | 파일 존재 | — | 미수정 |
| evaluate-session.js | ✗ 미등록 | 파일 존재 | — | 미수정 |
| mcp-health-check.js | ✗ 미등록 | 파일 존재 | — | 미수정 |

### 설정 이슈 (업데이트)

| # | 파일 | 이슈 | 변화 |
|---|------|------|------|
| H-1 | `hooks.json` | `/home/ubuntu` 19개 하드코딩 | ⚠️ 2주 연속 미수정 → 에스컬레이션 필요 |
| H-2 | `hooks.json` | `continuous-learning-v2` 스킬 없음 | ⚠️ 미수정 |
| H-3 | `hooks.json` | `dream` 스킬 없음 | ⚠️ 미수정 |
| H-4 | `settings.json` | deny 목록 없음, Bash(*) 전부 허용 | ⚠️ 미수정 |
| H-5 | `settings.json` | mcpServers 설정 없음 (프로젝트) | 변화 없음 |
| H-6 | `governance-capture.js` | `ECC_GOVERNANCE_CAPTURE=1` 미설정 | 🆕 신규 — 보안 캡처 완전 비활성 |

---

## 3. 스킬 건강

### 스킬 목록

| 위치 | 스킬 | 버전 | 7일 성공률 | 상태 |
|------|------|------|-----------|------|
| 프로젝트 로컬 | `korean-cover-letter-writer` | v1 | n/a | stable — 실행 이력 없음 |
| 프로젝트 로컬 | `ui-ux-pro-max` | v1 | n/a | stable — 실행 이력 없음 |
| 글로벌 | `session-start-hook` | v1 | n/a | stable |

**총 스킬 수: 3개** (로컬 2 + 글로벌 1)

### skills-health.js 실행 결과
```json
{
  "generated_at": "2026-04-12T15:35:18.478Z",
  "warn_threshold": 0.1,
  "skills": []
}
```
→ 실행 데이터 전무. 근본 원인: hooks.json 경로 오류로 `cost-tracker.js`, `session-end.js` 등이 실행 안 되어 metrics 축적 불가.

### 미사용 스킬
- `korean-cover-letter-writer`: 실행 이력 없음. CORTHEX 프로젝트 성격상 사용 빈도 극히 낮을 것으로 예상.
  - **권고**: 프로젝트 로컬에서 글로벌 스킬로 이동하거나 제거 검토.
- `ui-ux-pro-max`: 실행 이력 없으나 Phase 7 리디자인에서 사용 예정 → 유지.

### 누락 스킬 (hooks.json 참조 중)
- `continuous-learning-v2` — 없음 (2주 연속)
- `dream` — 없음 (2주 연속)

### 하락 추세
- 없음 (데이터 없어 판단 불가 — hooks 수정 후 자연 축적 필요)

---

## 4. 규칙 추출

### 커밋 패턴 분석 (2026-04-05 ~ 2026-04-12)

| 날짜 | chore(ecc) | quality | briefing | 기타 |
|------|-----------|---------|---------|------|
| 04-12 | **5** | — | 1 | — |
| 04-11 | **7** | 1 | 1 | — |
| 04-10 | 4 | 1 | 1 | — |
| 04-09 | **7** | 1 | — | — |
| 04-08 ~ 04-05 | (이전 구간) | — | — | — |
| **7일 합계** | **39** | ~4 | ~3 | — |

**이상 패턴**: `chore(ecc): 3h maintenance` 커밋이 하루 최대 7회 발생. 3시간 주기라면 하루 8회 max인데 7회는 정상 범위 상단 — 루프 중복 실행 가능성 있음.

### 신규 규칙 후보

| ID | 패턴 근거 | 제안 규칙 | 우선순위 |
|----|-----------|-----------|---------|
| R-1 | hooks.json 경로 오류 2주 미수정 | "hooks.json 경로 오류는 자동 수정 대상: `sed -i 's|/home/ubuntu/|/home/user/|g'` PreToolUse 훅으로 등록 또는 사용자에게 강제 알림" | 🔴 즉시 |
| R-2 | ECC_GOVERNANCE_CAPTURE 미활성 | "보안 캡처 기능은 환경변수 없이 기본 활성화로 설계 변경 권고 (opt-out 방식으로 전환)" | 🟠 높음 |
| R-3 | 하루 7회 ECC 유지보수 커밋 | "ECC 3h-maintenance 커밋이 이미 오늘 있으면 중복 실행 차단: `git log --oneline --since=midnight | grep 'chore(ecc)' | wc -l`로 체크" | 🟠 높음 |
| R-4 | skills-health.js → 빈 결과 | "감사 보고서에 skills-health.js 결과가 빈 배열이면 '데이터 없음'이 아닌 'hooks 비작동으로 미수집' 으로 분류" | 🟡 중간 |
| R-5 | continuous-learning-v2, dream 2주 미설치 | "누락 글로벌 스킬이 2회 이상 보고되면 해당 hooks.json 엔트리를 자동 주석처리 (silent fail 방지)" | 🟡 중간 |

### 지속 위반 패턴 (CLAUDE.md 규칙 有, 반복 무시됨)
- **경로 하드코딩 금지** (hooks.json 2주 연속 미수정) → 규칙 추가보다 자동화 픽스가 효과적
- **"완료" 거짓 보고** — 이전 감사에서도 동일 패턴 기록됨

---

## 5. 추천 액션

### 🔴 즉시 (이번 세션)

**[A-1] hooks.json 경로 수정 (2주 지연)**
```bash
sed -i 's|/home/ubuntu/corthex-v2|/home/user/corthex-v2|g' .claude/hooks.json
```
영향: 19개 훅 복원 — tsc 검증, party mode 강제, governance 캡처, 비용추적 한 번에 살아남.

**[A-2] ECC_GOVERNANCE_CAPTURE 활성화**
`~/.claude/settings.json` 또는 세션 환경에 `ECC_GOVERNANCE_CAPTURE=1` 추가. 현재 보안 이벤트 로그가 전혀 쌓이지 않고 있음.

### 🟠 이번 주

**[A-3] ECC 유지보수 중복 커밋 방지**
`chore(ecc)` 커밋이 당일 이미 존재하면 추가 커밋을 건너뛰는 로직 추가.
```bash
# dream/maintenance 스크립트에 삽입
TODAY_COMMITS=$(git log --oneline --since=midnight | grep -c "chore(ecc)" || echo 0)
[ "$TODAY_COMMITS" -ge 3 ] && echo "Already committed today, skipping" && exit 0
```

**[A-4] 누락 글로벌 스킬 처리**
`continuous-learning-v2`와 `dream` — 스킬 설치 또는 hooks.json에서 해당 엔트리 주석 처리.
현재 매 세션 종료 시 silent fail로 오류 발생 중.

### 🟡 다음 주

**[A-5] 미등록 ECC 훅 검토**
파일 존재하나 hooks.json 미등록:
- `quality-gate.js` → PostToolUse(Edit) 추가 검토
- `evaluate-session.js` → Stop 훅 추가 검토
- `mcp-health-check.js` → SessionStart 추가 검토

**[A-6] governance-capture 기본값 변경**
현재 opt-in (`ECC_GOVERNANCE_CAPTURE=1` 필요) → opt-out 방식으로 전환 권고.
보안 탐지 기능이 설치는 되어 있으나 플래그 미설정으로 한 번도 실행된 적 없음.

**[A-7] korean-cover-letter-writer 위치 검토**
CORTHEX 프로젝트 로컬에서 불필요 — 글로벌(`~/.claude/skills/`)로 이동하거나 제거.

---

## 요약

| 카테고리 | 상태 | 지난주 대비 | 주요 이슈 |
|----------|------|------------|-----------|
| 보안 | 🟡 주의 | 동일 | 실제 유출 없음, governance 캡처 비활성 |
| 훅 인프라 | 🔴 중단 | **악화** — 2주 미수정 | hooks.json /ubuntu 경로 19개, 대부분 훅 비작동 |
| 스킬 | 🟡 보통 | 동일 | 2개 존재, 실행 데이터 없음 |
| 커밋 패턴 | 🟡 주의 | 🆕 신규 | 하루 최대 7회 ECC 커밋, 중복 실행 가능성 |
| 규칙/거버넌스 | 🟡 보통 | 동일 | CLAUDE.md 규칙 충분, 자동 강제 불가 |

**핵심**: `hooks.json` 경로 1줄 `sed` 수정으로 19개 보호 기능이 복원됩니다.
2주째 동일 이슈가 방치되고 있음 — 이번 주 내 강제 수정 권고.
