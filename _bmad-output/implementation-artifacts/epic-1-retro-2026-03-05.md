# Epic 1 회고: 프로젝트 초기화 & 인프라

**날짜:** 2026-03-05
**참여:** Bob (SM), Alice (PO), Winston (Architect), Charlie (Senior Dev), Dana (QA), Elena (Junior Dev), ubuntu (Project Lead)

---

## Epic 요약

| 항목 | 수치 |
|------|------|
| 완료 스토리 | 8/8 (100%) |
| 단위 테스트 | 0 → 74건 |
| 빌드/타입 실패 | 0건 |
| 보안 취약점 발견/수정 | 8곳 (companyId 필터 누락) |
| 기술 부채 항목 | 6건 |

**구축된 인프라:**
1. DB: PostgreSQL + Drizzle ORM, 15개 P1 테이블 + 인덱스
2. API 서버: Hono 기반, 표준 응답 구조, Rate Limiting, Graceful Shutdown
3. 프론트엔드: React 19 + Vite 6 SPA 2개(admin/app), Zustand + TanStack Query
4. UI 라이브러리: @corthex/ui Phase A 컴포넌트 8개
5. 보안: AES-256-GCM Credential Vault, 테넌트 격리 자동 테스트
6. 배포: Docker ARM64 + GitHub Actions CI/CD + Cloudflare 캐시 퍼지
7. 실시간: WebSocket 5채널 멀티플렉싱 + EventBus
8. PWA: manifest.json + Service Worker 기본 설치

---

## 잘 된 점

1. **테넌트 격리 테스트로 보안 취약점 8곳 발견/수정** — chat.ts 3곳, reports.ts 3곳, jobs.ts 2곳에서 companyId 필터 누락을 코드 검토로 발견하여 수정. 보안 측면에서 가장 큰 성과
2. **테스트 0 fail 유지** — 모든 스토리에서 깨지는 테스트 없이 35→74건까지 안정적으로 증가
3. **Credential Vault 서비스 추상화** — provider별 JSONB 개별 암호화, 우선순위 로직(user→company) 중앙화
4. **EventBus→WS 브리지 패턴** — 서버 내부 이벤트를 실시간 브로드캐스트하는 아키텍처 확립
5. **배포 파이프라인 일회 구축** — GitHub Actions → Docker → Oracle VPS → Cloudflare 퍼지 자동화

---

## 어려웠던 점

1. **스키마 변경의 연쇄 수정** — tools→tool_definitions 리네임 시 13개 파일 수정 (1.2), encryptedKey→credentials JSONB 전환 시 다수 라우트 수정 (1.6)
2. **통합 테스트 CI 포함 불가** — tenant-isolation.test.ts 등 서버 실행 필요 테스트를 CI에 포함하지 못함
3. **문서와 코드 불일치** — epics 문서의 lib/encrypt.ts가 실제로는 lib/crypto.ts, src/tests/isolation.test.ts가 실제로는 src/__tests__/tenant-isolation.test.ts

---

## 핵심 교훈

1. **스키마 설계는 초기에 신중하게** — 이후 리네임/구조 변경 시 연쇄 수정 비용이 큼
2. **테넌트 격리 테스트는 필수** — 코드 리뷰만으로는 보안 취약점을 다 잡지 못함
3. **문서 경로는 코드와 일치시킬 것** — 기획 문서의 파일명이 실제와 다르면 개발 시 혼란 발생

---

## 기술 부채

| # | 항목 | 영향도 | 출처 |
|---|------|--------|------|
| 1 | Rate Limiter in-memory (수평 확장 시 Redis 필요) | 중 | 1.3 |
| 2 | 통합 테스트 CI 미포함 | 높음 | 1.7 |
| 3 | WebSocket 격리 테스트 skip 상태 | 중 | 1.7/1.9 |
| 4 | PWA 아이콘 placeholder | 낮음 | 1.8 |
| 5 | Service Worker 패스스루만 (P2 Web Push 예정) | 낮음 | 1.8 |
| 6 | WS 전용 단위 테스트 미작성 | 중 | 1.9 |

---

## 액션 아이템

1. 스키마 변경 전 영향 범위 분석을 습관화
2. 통합 테스트 CI 파이프라인 구축 검토 (별도 job으로 서버 기동 후 실행)
3. WebSocket 격리 테스트 skip 해제 확인
4. 기획 문서 내 파일 경로를 실제 코드와 정기 동기화
