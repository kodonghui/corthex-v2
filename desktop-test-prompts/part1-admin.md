Chrome에서 아래 단계를 순서대로 실행하세요. Admin: https://corthex-hq.com/admin (admin / admin1234)
각 단계 결과를 "작동/안됨/에러+내용"으로 보고해주세요.

중요: 어떤 단계에서 에러가 나서 뒤 단계가 불가능하면 (예: 온보딩 실패 → 에이전트 생성 불가),
그 지점에서 멈추고 아래를 보고해주세요:
1. 어디서 막혔는지 (URL, 단계 번호, 클릭한 버튼)
2. 에러 메시지 정확히 (화면 캡처)
3. 브라우저 콘솔 에러 (F12 → Console 탭)
4. 뒤에 못 한 단계 목록

## PART 1: ADMIN 패널 (약 20분)
URL: https://corthex-hq.com/admin

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 1-1. 로그인 (1분)
1. Chrome에서 https://corthex-hq.com/admin/login 접속
2. 관리자 ID: admin, 비밀번호: admin1234 입력
3. "세션 시작" 클릭
4. 대시보드가 나오면 성공
5. 잘못된 비밀번호로도 한번 시도해보기 → 에러 메시지 확인

### 1-2. 온보딩 (3분)
왼쪽 사이드바 → "온보딩" 클릭 (또는 /admin/onboarding)

**Step 1 — 회사 설정:**
- 기본 시드로 "CORTHEX HQ" 회사가 이미 있음
- "Edit" 클릭 → 원하는 회사명(예: "테스트컴퍼니")으로 변경 → "Save" 클릭
- 토스트 메시지 "회사명이 수정되었습니다" 확인
- (참고: 회사가 없으면 온보딩이 안 됨 — seed가 필수)

**Step 2 — 부서 설정:**
- 조직 템플릿 목록이 보임 (투자분석, 마케팅, 올인원 등)
- "올인원" 템플릿의 "Apply" 클릭 → 에이전트 자동 생성되는지 확인
  - 만약 에러 나면: "빈 조직으로 시작" 클릭
- 커스텀 부서 추가: "영업팀" 입력 → "Add" 클릭 → 부서 추가 확인
- 빈 이름으로 "Add" 클릭 → 아무것도 안 되는지 확인

**Step 3 — API 키:**
- Anthropic API 키 입력란에 키 입력 → "등록" 클릭
- "등록됨" 표시 확인
- "Continue" 클릭

**Step 4 — 팀원 초대:**
- 아이디: "testuser", 이름: "테스트유저", 이메일: "test@test.com" 입력
- "초대하기" 클릭 → 임시 비밀번호가 표시되는지 확인
- "Copy" 버튼 → 복사되는지 확인
- "Skip for now" 클릭

**Step 5 — 완료:**
- 요약 정보 확인 (회사명, 템플릿, API 키, 초대 직원 수)
- "CORTHEX 사용 시작하기" 클릭 → 대시보드로 이동

### 1-3. 대시보드 (2분)
/admin → 대시보드 확인

**확인 항목:**
- 상단 3개 카드: DEPARTMENTS (숫자), ACTIVE USERS (숫자), AUTONOMOUS AGENTS (숫자)
- Health Status 섹션: 활성 사용자 %, 온라인 에이전트 %, 부서 수
- Recent Activity 테이블: USER/AGENT 목록
- Department Overview: 부서 목록
- Agent Efficiency Readout: 원형 게이지

**테스트:**
- "로그 내보내기" 버튼 클릭 → 뭔가 되는지 확인
- "전체 기록 보기" 버튼 클릭 → 뭔가 되는지 확인

### 1-4. 회사 관리 (3분)
사이드바 → 조직 관리 → 회사 관리

**CRUD 테스트:**
1. "Create Company" 클릭 → 이름: "테스트회사2", 슬러그: "test-company-2" → 생성
2. 생성된 카드 확인
3. 검색창에 "테스트" 입력 → 필터 작동 확인
4. 편집 (연필 아이콘) → 이름 변경 → 저장
5. 삭제 (쓰레기통 아이콘) → 확인 다이얼로그 → 삭제
6. 중복 슬러그로 생성 시도 → 에러 메시지 확인

### 1-5. 직원 관리 (3분)
사이드바 → 조직 관리 → 직원 관리

**CRUD 테스트:**
1. "직원 추가" 클릭 → 아이디: "emp1", 이름: "테스트직원", 이메일: "emp1@test.com" → 생성
2. 임시 비밀번호 모달 확인 → 복사 버튼 테스트
3. 검색: "테스트" 입력 → 필터 확인
4. 부서 필터 드롭다운 → 부서 선택 → 필터 확인
5. 상태 필터 (active/inactive) 테스트
6. 편집 (연필 아이콘) → 이름 변경 → 저장
7. 비밀번호 초기화 (열쇠 아이콘) → 확인 → 새 비밀번호 표시
8. 비활성화 (자물쇠 아이콘) → 확인 → 상태 변경

### 1-6. 부서 관리 (2분)
사이드바 → 조직 관리 → 부서 관리

**CRUD 테스트:**
1. "Create Department" → 이름: "QA팀", 설명: "품질 관리" → 생성
2. 검색: "QA" → 필터 확인
3. 행 클릭 → 상세 패널 열림 (부서명, ID, 설명, 에이전트 목록)
4. 인라인 수정 (연필 아이콘) → 설명 변경 → 저장
5. 삭제 (쓰레기통 아이콘) → cascade 분석 모달 확인 (소속 에이전트 수, 비용 등)
6. "강제 종료" 선택 → 삭제 실행 → 상태 "Inactive" 변경

### 1-7. AI 에이전트 관리 (5분) ★★★ 핵심
사이드바 → AI 관리 → AI 에이전트

**에이전트 생성:**
1. "NEW AGENT" 클릭
2. 아래 에이전트를 하나 만들어보세요:
   - 이름: QA분석가
   - 역할: 품질 분석 전문가
   - 티어: Specialist
   - 모델: Claude Haiku 4.5
   - 부서: (아무거나 선택)
   - 소울: "당신은 품질 분석 전문가입니다. 데이터를 분석하고 개선점을 찾습니다."
3. 생성 후 목록에서 확인

**필터 테스트:**
4. Filter_Tier 드롭다운 → "MANAGER" 선택 → Manager 에이전트만 보이는지 확인
5. Filter_Status 드롭다운 → "ONLINE" 선택 → 온라인 에이전트만 보이는지 확인
6. 검색창에 이름 입력 → 필터 확인

**상세 패널:**
7. 에이전트 행 클릭 → 상세 패널 열림
8. 탭 확인: Soul, Config, Memory
9. Soul 탭: 소울 텍스트 확인
10. Config 탭: 설정 확인 (Semantic Cache 토글)
11. Memory 탭: 메모리 현황 확인

**수정:**
12. 이름 변경 → PATCH 저장 확인
13. Semantic Cache 토글 → 즉시 반영 확인

**비활성화:**
14. "Deactivate Agent" 클릭 → 확인 → 상태 변경

**영구 삭제 (Hard Delete):**
15. 비활성화된 에이전트에서 "Permanently Delete" 버튼 → 확인 → 목록에서 사라짐

### 1-7b. 직원 부서 배치 + 권한 테스트 (3분) ★★ 중요
이 테스트는 "인간 직원이 자기 부서 에이전트만 볼 수 있는지"를 확인합니다.

**직원 생성 + 부서 배정:**
1. /admin/employees → "직원 추가"
2. 아이디: "sales01", 이름: "영업팀 김직원", 이메일: "sales@test.com"
3. 부서 드롭다운에서 특정 부서 1개만 선택 (예: 마케팅팀)
4. 생성 → 임시 비밀번호 기억/복사

**직원 계정으로 App 로그인:**
5. 새 시크릿 탭 또는 새 Chrome 프로필에서 https://corthex-hq.com/login 접속
6. 아이디: "sales01", 비밀번호: (복사한 임시 비밀번호)
7. 로그인 성공 확인

**부서 스코프 확인:**
8. /agents 페이지 → 자기 부서(마케팅팀) 에이전트만 보이는지 확인
9. /departments 페이지 → 자기 부서만 보이는지 확인
10. /chat → 에이전트 선택 시 자기 부서 에이전트만 나오는지 확인

**부서 미배정 직원 테스트:**
11. Admin으로 돌아가서 → 새 직원 "noname01" 생성 (부서 선택 안 함)
12. "noname01"으로 로그인 → 모든 에이전트가 보이는지 확인 (부서 미배정 = 전체 접근)

**CEO vs 직원 비교:**
13. CEO 계정 (ceo/ceo1234)으로 로그인 → 모든 에이전트/부서 접근 가능 확인
14. 직원 계정은 제한적, CEO는 전체 — 차이 확인

### 1-8. 도구 관리 (2분)
사이드바 → AI 관리 → 도구 관리

**확인:**
1. 도구 목록 확인 (카테고리별)
2. 검색: "search" → 웹 검색 도구 필터
3. "Register Tool" → 새 도구 등록 폼 확인
4. Agent Permission Matrix: 에이전트별 도구 체크박스 확인
5. 체크박스 변경 → 저장 → 토스트 확인

### 1-9. 비용 관리 (2분)
사이드바 → 운영 → 비용 관리

**확인:**
1. 시간 필터 (24H, 7D, 30D, ALL) 클릭 → 기간 변경
2. 3개 탭 (부서별, 에이전트별, 모델별) 전환
3. 예산 설정: 금액 입력 → 저장 → 프로그레스 바 표시
4. Export CSV 클릭

### 1-10. CLI / API 키 (Credentials) (2분)
사이드바 → 운영 → CLI / API 키

**확인:**
1. 사용자 선택 드롭다운
2. CLI Token 섹션 확인
3. API 키 섹션:
   - 프로바이더 드롭다운 → "anthropic" 선택 → API Key 필드 1개 확인
   - 프로바이더 → "kis" 선택 → App Key + App Secret + Account No 3개 필드 확인
   - 프로바이더 → "telegram" 선택 → Bot Token + Chat ID 2개 필드 확인
4. 실제로 하나 등록해보기 (serper: api_key 입력 → 등록)
5. 삭제: 등록한 키 삭제 → 확인 다이얼로그 → 삭제

### 1-11. 보고 라인 (2분)
사이드바 → 조직 관리 → 보고 라인

**확인:**
1. 직원 목록과 드롭다운 확인
2. 드롭다운에서 상사 선택 → "변경됨" 표시
3. "저장" 클릭 → 토스트
4. 페이지 새로고침 → 저장된 데이터 유지 확인 ★(이전에 안 됐던 버그)
5. 새 보고 라인 추가: Reporter + Supervisor 선택 → "추가" 클릭

### 1-12. 소울 템플릿 (1분)
사이드바 → AI 관리 → 소울 템플릿

**CRUD 테스트:**
1. "Create Template" → 이름: "테스트소울", 내용: "당신은 테스트 에이전트입니다." → 생성
2. 검색 → 필터 확인
3. 수정 (연필) → 내용 변경 → 저장
4. 게시 (Publish) / 비게시 토글
5. 삭제 → 확인 → 사라짐

### 1-13. 시스템 모니터링 (1분)
사이드바 → 운영 → 시스템 모니터링

**확인:**
1. Server Status: ONLINE/OFFLINE
2. Uptime 표시
3. Memory 사용량 게이지
4. DB Latency 표시
5. SYS-LOG (시스템 로그) 목록
6. "Refresh" 버튼 → 데이터 갱신

### 1-14. NEXUS 조직도 (1분)
사이드바 → 조직 관리 → NEXUS 조직도

**확인:**
1. ReactFlow 캔버스 로딩
2. 에이전트/부서 노드 표시
3. 줌 인/아웃
4. Fit View 버튼
5. 노드 클릭 → 속성 패널
6. Export 버튼 → JSON 다운로드

### 1-15. 설정 (1분)
사이드바 하단 → 회사 설정

**확인:**
1. General 탭: 회사명 표시, 수정 가능
2. API Keys 탭: 등록된 키 목록
3. Agent Settings 탭: Handoff Depth 슬라이더

