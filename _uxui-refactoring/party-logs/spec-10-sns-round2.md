# Party Mode: spec-10-sns Round 2

## 참여 전문가
| 이름 | 역할 | 관점 |
|------|------|------|
| John | PM | 적대적 -- v1 기능 누락 재검증 |
| Winston | Architect | 적대적 -- 구조적 결함 탐색 |
| Sally | UX | 적대적 -- 사용성 함정 탐색 |
| Amelia | Dev | 적대적 -- testid 구멍 탐색 |
| Quinn | QA | 적대적 -- 엣지케이스 스트레스 |
| Bob | SM | 적대적 -- 범위 초과 위험 탐색 |
| Mary | BA | 적대적 -- 비즈니스 가치 의문 |

## Round 1 수정사항 검증
- [x] 이슈 #1: v1 플랫폼 목록 수정 -- 현재 스펙 1번, 9번, 10번에서 Instagram, YouTube, 티스토리, 다음카페, LinkedIn으로 정확히 기재됨. **검증 완료**
- [x] 이슈 #2: 로딩/에러 testid 추가 -- `sns-loading`, `sns-error-state` 추가됨. **검증 완료**
- [x] 이슈 #3: Playwright 테스트 19~20번 -- 로딩/에러 시나리오 추가됨. **검증 완료**

## 리뷰 결과

### John (PM)
Round 1에서 수정된 플랫폼 목록을 재확인했습니다. 스펙 전체를 다시 대조하면, "Selenium 기반 자동 발행"이 v1-feature-spec 8.2에 있었는데 스펙에 직접 언급이 없습니다. 다만 이것은 백엔드 발행 엔진이므로 UI 스펙 범위 밖입니다. 나머지 v1 기능(콘텐츠 생성, 승인/반려, 예약 큐, 카드뉴스, A/B, 이미지 생성, 계정 CRUD, 배치, 통계)은 모두 커버 확인.

### Winston (Architect)
Banana2 프롬프트를 다시 검토했습니다. 데스크톱 프롬프트에서 "Instagram, blog, Twitter, Facebook"이라고 되어 있는데, Round 1에서 지적한 플랫폼 목록 수정이 Banana2 프롬프트에는 반영되지 않았을 수 있습니다. 실제 스펙의 프롬프트를 확인하니 프롬프트 내용이 제네릭("multiple platforms")으로 쓰여 있어 큰 문제는 아니지만, 플랫폼 이름이 명시된 부분은 일관성 확인이 필요합니다.

### Sally (UX)
갤러리 뷰에서 이미지 없는 콘텐츠의 플레이스홀더 디자인이 여전히 추상적입니다. "플레이스홀더 썸네일로 표시"라고만 되어 있는데, 이것이 플랫폼 아이콘인지, 회색 박스인지, 텍스트 미리보기인지 불명확합니다. 구현 시 Banana2가 결정하므로 스펙 수준에서는 충분합니다.

### Amelia (Dev)
Round 1에서 추가된 `sns-loading`과 `sns-error-state`를 확인했습니다. 카드뉴스 상세 뷰(`CardNewsDetail`)에서 개별 카드 슬라이드의 testid가 없습니다. 카드뉴스 시리즈 내 개별 카드를 테스트하려면 `sns-cardnews-slide` 같은 testid가 필요할 수 있으나, 이것은 구현 시 추가해도 무방합니다.

### Quinn (QA)
에러/로딩 테스트가 추가된 것을 확인했습니다. 추가 엣지케이스: 이미지 생성 버튼 클릭 후 AI 이미지 생성이 실패할 때의 에러 UI가 명시되어 있지 않습니다. 이것은 API 에러의 일반적인 처리(토스트 알림)로 커버될 수 있으므로 별도 정의까지는 불필요합니다.

### Bob (SM)
카드뉴스 드래그앤드롭 순서 변경이 "검토"에서 변경되지 않았습니다. 실제 구현 시 dnd-kit 도입이 필요한지 여부를 사전에 결정하면 좋겠지만, UI 스펙 단계에서는 "검토"로 충분합니다.

### Mary (BA)
통계 탭의 차트가 "div 높이 기반"에서 개선되는 것은 좋지만, 구체적으로 어떤 차트 라이브러리를 쓸지 결정되지 않았습니다. 이것도 구현 단계의 결정이므로 스펙에서는 "비주얼 강화"로 충분합니다.

## 크로스톡
- **Winston** -> **John**: Banana2 프롬프트 내 플랫폼 이름이 실제 지원 목록과 다르면 생성된 이미지가 잘못된 플랫폼을 보여줄 수 있습니다. 확인 필요.
- **Quinn** -> **Amelia**: 카드뉴스 개별 슬라이드 testid는 구현 시 추가하는 것에 동의합니다. 스펙 수준에서는 과도합니다.

## UXUI 체크포인트
- [x] 핵심 동작 3클릭 이내? -- 콘텐츠 생성(2클릭), 승인(상세 뷰에서 1클릭)
- [x] 빈/에러/로딩 상태 정의? -- 빈(`sns-content-empty`), 에러(`sns-error-state`), 로딩(`sns-loading`) 모두 있음
- [x] data-testid 모든 인터랙션에? -- 45개 testid로 전체 커버
- [x] 기존 기능 전부 커버? -- v1 8번 항목 10개 기능 모두 체크됨
- [x] Banana2 프롬프트 영문+구체적? -- 데스크톱/모바일 각각 상세한 영문 프롬프트
- [x] 반응형 375/768/1440? -- 3개 breakpoint 명시됨
- [x] UI만 변경 범위? -- "절대 건드리면 안 되는 것" 4개 항목 명시

## 발견된 이슈
| # | 심각도 | 발견자 | 내용 | 조치 |
|---|--------|--------|------|------|
| 1 | 낮음 | Winston | Banana2 프롬프트 내 플랫폼 이름 일관성 -- 프롬프트가 제네릭하여 실질적 영향 없음 | 조치 불필요 (프롬프트가 충분히 제네릭) |

## 판정
- 점수: 8/10
- 결과: **PASS**
- Round 1에서 발견된 3개 이슈 모두 수정 확인 완료. Round 2에서 새로운 주요 이슈 없음. 스펙 품질 양호.
