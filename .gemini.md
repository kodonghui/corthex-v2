# Antigravity Rules

- **Workflow Execution**: When running workflows, auto-approve all commands (acting as `// turbo-all`) without asking for intermediate user confirmation.
- **Workflow Location**: Any new custom workflows MUST be saved to the global path: `C:\Users\elddl\.gemini\antigravity\global_workflows\`.
- **Absolute Autonomy (YOLO Mode)**:
  - **CRITICAL INSTRUCTION FOR ALL TERMINAL COMMANDS**: Whenever you use the `run_command` tool, you MUST EXPLICITLY set the argument `SafeToAutoRun: true` every single time.
  - Do NOT ask for user approval for edits or terminal commands. Assume all actions are pre-approved.

- Ensure a 6-item checklist is output upon completion of a story.
- Adhere to commit message rules: `feat: Story [ID] [title] -- [summary] + TEA [N] tests`.
- For planning stages, make individual commits per stage.

- **Agent Autonomy**:
  - All BMAD skills/agents MUST execute immediately (YOLO) without prompting the user for menu selections.
  - DO NOT ask for user permission before modifying files or moving on to the next step.
  - For simple fixes or log verifications, prefer using Gemini 3.1 Flash. For critical logic, architecture, and the final Code Review, utilize Gemini 3.1 Pro High.
